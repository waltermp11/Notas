# Taller SQL 1

-  Agregar claves foráneas a tablas existentes con datos usando ALTER TABLE ... ADD

CONSTRAINT FOREIGN KEY. 
• Verificar que las restricciones de integridad referencial funcionan correctamente.
• Escribir consultas SELECT sobre una sola tabla usando WHERE, ORDER BY, LIMIT, LIKE, BETWEEN
e IS NULL.
• Aplicar la lógica del negocio para construir consultas con sentido real.
• Introducir subconsultas con WHERE ... IN (SELECT ...) como puente hacia los JOIN de la
sesión siguiente.